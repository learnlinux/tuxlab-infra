{
	"ssh_port" : 22,
	"etcd_conf" : {
		"hosts" : ["172.17.0.1:2379"]
	},
  "webssh_port" : 80,
  "webssh_root" : "{{ WEBSSH_ROOT }}",
  "webssh_http_redirect_url" : "{{ WEBSSH_REDIRECT }}"
  "docker_host_ip" : "{{ ansible_ssh_host }}" 
  "docker_host_port" : 2375
  {% if proxy_pass is defined %},
  "etcd_user" : 'proxy',
  "etcd_pass" : {{ proxy_pass }}
  {% endif %}
}
