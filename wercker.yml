box: ubuntu
build:
  steps:
    - install-packages:
        packages: virtualbox vagrant make dkms

    - script:
        name: finalize installation of vagrant
        shell: |
          sudo apt-get install linux-headers-generic

    - script:
        name: create environment
        code: vagrant up

    - script:
        name: run tests
        code: make test > $WERCKER_REPORT_ARTIFACTS_DIR/test_results.log
